import Player from './player.tsx';

const Home = () => (
  <main className="
    flex
    flex-col
    w-screen
    h-screen
    items-center
    justify-center">
    <Player />
  </main>
); export default Home;
