import Player from './player.tsx';

const Home = () => {
  return (
    <main className="
      flex
      flex-col
      w-screen
      h-screen
      items-center
      justify-center"
    >
      <Player />
    </main>
  );
};

export default Home;
